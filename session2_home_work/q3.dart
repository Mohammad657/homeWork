//Q3- Declare a var variable and assign a string value to it. Then reassign an integer to the same
//variable. Print the variable after each assignment.


void main() {
  var stringToInt;
  stringToInt = 'Welcome';
  print(stringToInt);
  stringToInt = 42;
  print(stringToInt);
}
